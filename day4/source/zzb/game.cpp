#include <cstdio>
#include <vector>
#include <queue>
#include <map>
using namespace std;
int t;
int main() {
	freopen("game.in", "r", stdin);
	freopen("game.out", "w", stdout);
	scanf("%d", &t);
	while(t--) {
		printf("A\n");
	}
	return 0;
}
